#include<bits/stdc++.h>
using namespace std;
int main()
{
    vector<int> num={1,5,3,2,8,7,5};
    int n=num.size();
    int temp=num[0];
    for(int i=0;i<n-1;i++)
    {
        num[i]=num[i+1];
    }
    num[n-1]=temp;
    for(int i=0;i<n;i++)
    {
        cout<<num[i]<<" ";
    }
    return 0;
}
