#include<bits/stdc++.h>
using namespace std;
int main()
{
    vector<int> num={1,2,5,6,8,2,9,2,9};
    int n=num.size();
    sort(num.begin(),num.end());
    for(int i=0;i<n;i++)
    {
        cout<<num[i];
    }
    return 0;
}

