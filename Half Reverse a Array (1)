#include<bits/stdc++.h>
using namespace std;
int main()
{
    vector<int> num={1,2,3,4,5,6};
    for(int index=0;index<(index;index++)
    {
        cout<<num[index]<<" ";
    }
    for(int index=5;index>=3;index--)
    {
        cout<<num[index]<<" ";
    }
    return 0;
}
