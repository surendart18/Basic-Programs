#include<bits/stdc++.h>
using namespace std;
int main()
{
    vector<int> num={1,2,3,4,5};
    int n=num.size();
    int sum=0;
    for(int i=0;i<n;i++)
    {
      sum=num[i]+sum;  
    }
    cout<<sum;
    return 0;
}
